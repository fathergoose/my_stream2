require 'rails_helper'

RSpec.describe Track, type: :model do
  pending "add some examples to (or delete) #{__FILE__}"
  it "is valid with a path, artist_id, album_id, title, track_number, quality_id, and user_id"
  it "is invlaid without a path"
end
